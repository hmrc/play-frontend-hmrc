This fixture is excluded because we have decided not to implement the functionality from `govuk-frontend` to exclude a `day`, `month` or `year` by setting to `false`.

This decision was made during the upgrade to `govuk-frontend` v6.4.0. We decided that adding this functionality would create a more complicated viewmodel than we want to support at this time.
