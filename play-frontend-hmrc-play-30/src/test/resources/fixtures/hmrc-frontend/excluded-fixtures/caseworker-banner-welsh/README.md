This fixture is excluded because it exists only to test passing an explicit language parameter in hmrc-frontend.  In play-frontend-hmrc,
we use an implicit messages instance to translate use the request, and test this in the HmrcCaseworkerBannerSpec.
